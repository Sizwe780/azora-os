import { useState, useEffect } from "react";
import { fetchMonetizationSubscription } from "../services/monetization-subscription";

export default function useMonetizationSubscriptionApi(payload) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);
  useEffect(() => {
    setLoading(true);
    fetchMonetizationSubscription(payload)
      .then(setData)
      .catch(setError)
      .finally(() => setLoading(false));
  }, [JSON.stringify(payload)]);
  return { data, loading, error };
}
