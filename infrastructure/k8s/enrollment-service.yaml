apiVersion: apps/v1
kind: Deployment
metadata:
  name: enrollment-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: enrollment-service
  template:
    metadata:
      labels:
        app: enrollment-service
    spec:
      containers:
      - name: enrollment-service
        image: azora-enrollment-service:latest
        ports:
        - containerPort: 8083
        env:
        - name: DATABASE_URL
          value: "postgres://azora_user:password@azora-postgres-rw:5432/azora"
        livenessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: enrollment-service
  namespace: default
spec:
  selector:
    app: enrollment-service
  ports:
  - port: 8083
    targetPort: 8083
  type: ClusterIP