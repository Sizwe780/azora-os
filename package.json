{
  "name": "azora-os",
  "private": true,
  "scripts": {
    "start": "docker-compose up --build",
    "build": "docker-compose build",
    "lint": "eslint . --ext js,jsx",
    "test": "jest",
    "dev": "concurrently \\"docker-compose up\\" \\"cd apps/main-app && npm start\\""
  },
  "workspaces": ["apps/*", "services/*", "shared/*", "ui-overhaul/*"],
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "concurrently": "^8.0.0"
  }
}
