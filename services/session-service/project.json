{
  "name": "session-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/session-service",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd services/session-service && go build -o dist/session-service ."
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd services/session-service && go test ./..."
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd services/session-service && golangci-lint run"
      }
    }
  },
  "tags": ["scope:session", "type:service"]
}