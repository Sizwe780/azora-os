{
  "buildCommand": "cd /vercel/path0 && npm install && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": null,
  "ignoreCommand": "exit 0",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
